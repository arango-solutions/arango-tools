apiVersion: v2
name: arango-tools
description: |
  Single-origin GUI for the ArangoDB client tools (BYOC). Bundles the arango*
  client binaries and serves the React UI + FastAPI backend from one container.
type: application
# Chart version (bump on chart changes).
version: 0.1.0
# Version of the application packaged (matches the container image / app).
appVersion: "0.1.0"
keywords:
  - arangodb
  - tools
  - byoc
home: https://docs.arango.ai/arangodb/stable/components/tools/
